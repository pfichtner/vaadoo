<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>io.github.pfichtner</groupId>
		<artifactId>vaadoo</artifactId>
		<version>0.0.1-alpha-1-SNAPSHOT</version>
	</parent>

	<artifactId>vaadoo-bytebuddy-tests</artifactId>
	<packaging>pom</packaging>
	<name>Vaadoo - ByteBuddy - Tests</name>

	<properties>
		<maven.deploy.skip>true</maven.deploy.skip>
	</properties>

	<modules>
		<module>vaadoo-bytebuddy-tests-core</module>
		<module>vaadoo-bytebuddy-tests-vaadoo</module>
		<module>vaadoo-bytebuddy-tests-parent</module>
	</modules>

	<dependencies>
		<!-- for redefining already loaded classes in test -->
		<dependency>
			<groupId>net.bytebuddy</groupId>
			<artifactId>byte-buddy-agent</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<source>11</source>
					<target>11</target>
				</configuration>
			</plugin>
		</plugins>
		<pluginManagement>
			<!-- Configure the byte-buddy transform once for all test modules. Each
			     test module should execute the transform in process-classes so the
			     module's target/classes are instrumented before tests run. -->
			<plugins>
				<plugin>
					<groupId>net.bytebuddy</groupId>
					<artifactId>byte-buddy-maven-plugin</artifactId>
					<configuration>
						<classPathDiscovery>true</classPathDiscovery>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>

</project>
